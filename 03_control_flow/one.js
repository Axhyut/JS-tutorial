//if

// if (condition) {
    
// }

//if the condition is true then the if function will execute 
//if the condition is false then the code inside the scope will not get executed

const score = 200
if(score > 100){
    const power = "fly"
    console.log(`User power: ${power}`);
}