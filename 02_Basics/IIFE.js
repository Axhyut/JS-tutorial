//Immediately Invoked Function Expression (IIFE)
//(function declaration)(function call)
//it is used when the function that is declared should be consoled immediately

(function chai() {
    console.log(`DB CONNECTED`);
})()